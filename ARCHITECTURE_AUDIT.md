# ARCHITECTURE AUDIT — البوت القبطي التعليمي (خادم)
### من «خادم AI» (ملف واحد + مفتاح Gemini) → منصة تعليمية قبطية v3 (بلا أي مفتاح)

> نتيجة الفحص المعماري للمشروع **قبل** التعديل وكيف عالجناه.
> **آخر تحديث:** v3 — بعد إعادة البناء وإزالة مفتاح Gemini.

---

## 1) Project Map — خريطة المشروع

| الملف المرفوع | الوصف | الحالة الآن |
|---|---|---|
| `index.html` (17KB قديمًا) | المشروع كامل في ملف واحد: HTML+CSS+JS وكل البيانات مضمّنة | ← أُعيد بناؤه كـ **Shell فقط** (بلا منطق) + 30 ملفًا منظمًا |
| `الخطوات.txt` | دليل GitHub → Pages → مفتاح Gemini → PWA Builder | ← المفتاح **أُلغي**، والخطوات مدمجة في `README.md` |
| `ARCHITECTURE_AUDIT.md` | الفحص الأولي | ← هذا الملف (محدَّث) |
| `DEVELOPMENT_PLAN.md` | خطة المراحل | ← محدَّث لـ 20 مرحلة |
| `README.md` | التوثيق | ← محدَّث بالكامل |

### الحالة الفعلية للنسخة الأصلية
- **اللغة:** JavaScript (Vanilla ES6) + HTML5 + CSS3 — **لا Framework**.
- **التشغيل:** ملف ثابت واحد. لا Bot Server ولا Webhook.
- **قاعدة البيانات:** لا توجد — البيانات مصفوفات مضمّنة (`hymns`, `syn`, `course`, `quiz`).
- **التخزين:** `localStorage` بمفتاح واحد: `gemini_key`.
- **AI:** Gemini `gemini-2.5-flash` عبر `fetch` بمفتاح المستخدم.
- **الأمان:** لا يوجد. لا أسرار في الكود (جيد) لكن مفتاح المستخدم محفوظ محليًا.
- **Assets:** لا شيء (لا صور ولا خطوط).

---

## 2) القرار المعماري الأهم في v3

> كان المشروع يعتمد على مفتاح Gemini للمساعد. **حُذف كليًا.**

**السبب:** المفتاح كان يعني: تكلفة/حصة يومية، حاجة لتسجيل من Google، إرسال سؤال المستخدم لطرف ثالث، متطلب إنترنت، واحتمال اختلاق روابط ومصادر.

**البديل:** مساعد محلي (`js/core/bot.js`) يبني فهرسًا من محتوى المشروع نفسه، ويجيب منه فقط، **ويعرض المصادر دائمًا**، **ويعتذر** لو المعلومة غير موجودة.

| | مساعد v3 المحلي | مفتاح Gemini سابقًا |
|---|---|---|
| تكلفة | **صفر** | حصة يومية |
| مفتاح | **لا يوجد** | مطلوب |
| خصوصية | **كل شيء محلي** | السؤال يُرسل لطرف ثالث |
| إنترنت | **غير مطلوب** | مطلوب |
| اختلاق مصدر | **مستحيل** | ممكن |
| التوثيق | **يذكر العناصر المستخدمة** | لا يذكر |

---

## 3) الميزات الأصلية — كلها محفوظة ✅

| الميزة الأصلية | الحالة |
|---|---|
| شات AI | ✅ محوّل لمساعد محلي موسّع (بلا مفتاح) |
| مكتبة ألحان (5) | ✅ موسّعة إلى **25** + 3 نغمات + رتبة القداس + خطوات حفظ |
| السنكسار (3 مدخلات) | ✅ محفوظ كتقويم + مدخلات موثّقة (والناقص `NEEDS_ADMIN_INPUT`) |
| المنهج القبطي (10 بنود) | ✅ محفوظ وموسّع (`c_curriculum_10`) |
| لعبة الأسئلة (7 أسئلة) | ✅ محوّلة إلى محرك بـ 5 أوضاع + 85+ سؤالًا |
| التنقل بدالة `go(id)` | ✅ محوّل إلى **Router بالـ hash** (روابط قابلة للمشاركة) |

**لم يُحذف أي شيء يعمل** — نُقل وطُوّر.

---

## 4) البنية الجديدة (Modular)

```
index.html          Shell فقط — بلا منطق وبلا سكربت مضمّن (أمان + صيانة)
tests.html          80+ اختبارًا · 404.html · manifest.json · .nojekyll
SECURITY.md         سياسة الأمان
css/                style.css (ثيم داكن/فاتح) · responsive.css
js/config.js        إعدادات مركزية
js/core/            util · store · analytics · api · ui · bot · search
js/data/            sources · hymns · coptic-lang · content · liturgy · questions
js/games/           quiz · games · leaderboard
js/views/           router + 14 صفحة
js/app.js           Bootstrap
.tables/schema.json 6 جداول
```

**مبادئ البنية:**
1. **فصل تام** بين البيانات (data/) والمنطق (games/, core/) والعرض (views/).
2. **لا منطق في HTML** — ولهذا لا يوجد أي `<script>` مضمّن (أسرع وأأمن).
3. **كل وحدة في نطاق مغلق** (`IIFE`) تُصدّر كائنًا واحدًا على `window`.
4. **ترتيب التحميل موثّق** داخل `index.html` بتعليق صريح.

---

## 5) المخاطر ومعالجتها

| الخطر | المعالجة في v3 |
|---|---|
| **قيد جذري: موقع ثابت** | موثّق بوضوح في `README` + `SECURITY.md` + داخل صفحة «المزيد» كتنبيه صريح (بلا تمويه) |
| مفتاح المستخدم في `localStorage` | **أُلغي المفتاح** + تنظيف تلقائي لأي مفتاح قديم عند أول تشغيل |
| اختلاق المساعد لمصادر/روابط | لا يتصل بلا LLM — يجيب من فهرس داخلي + اختبار يتحقق من عدم وجود روابط غير معروفة |
| إدخال HTML ضار (XSS) | `Util.esc` في كل إدراج + `UI.*` مساعدات تهرّب + اختبارات XSS + `UI.rich` يهرّب **قبل** التنسيق |
| روابط خطرة (`javascript:`) | `Util.safeUrl()` يرفض أي شيء غير `https/http/#/mailto/tel` + اختبار |
| إساءة إدخال (نص ضخم) | `APP.LIMITS` لكل حقل + `Util.clampText` |
| كشف الإجابة قبل اللعب | `Bank.public()` + اختبار يفشل لو تسرّبت |
| تكرار الروابط في عشرات الملفات | ملف واحد `js/data/sources.js` + اختبار يتحقق من تسجيل كل مصدر |
| نصوص قبطية غير مدقّقة | لا تُقدَّم كمرجع: كل لحن معلَّم `needs_review` بشارة ظاهرة |
| السنكسار نصوص مقطوعة | لا نختلق — مدخلات موثّقة فقط والبقية بوسم `NEEDS_ADMIN_INPUT` |
| حقوق النشر | مقتطفات قصيرة فقط + اختبار يمنع المقتطفات الطويلة (>220 حرفًا للحن، >400 للآية) |
| بوابة Admin تُقدم كأمان | تنبيه صريح في الصفحة + `SECURITY.md` + README |
| فقدان بيانات الإنتاج عند تغيير الجداول | لا `rebuild_db` — نشر عادي يضيف الجداول/الأعمدة الناقصة |
| فشل الشبكة/القاعدة | كل `fetch` يلتقط الفشل ويعمل الموقع بالبيانات المحلية (يعمل كاملًا على GitHub Pages) |
| انهيار عند خطأ غير متوقع | `Util.guard` + `try/catch` حول كل صفحة + `window.onerror` + `unhandledrejection` |

---

## 6) قاعدة «لا تخمين» — الحالات المُعلَّمة

| الوسم | المعنى | مواضعه |
|---|---|---|
| `📝 NEEDS_ADMIN_INPUT` | معلومة تحتاج إدخالًا/تدقيقًا بشريًا | نطق ونغمة الألحان · مدخلات السنكسار · فهارس بعض الطقوس · روابط مدارس الشمامسة والواتساب · بعض الأسئلة |
| `🔗` مصدر | كل عنصر مرتبط بمصدر مسجَّل | كل الألحان والمحتوى والأسئلة |
| `🖥 NEEDS_BACKEND` | مستحيل في موقع ثابت | مصادقة إدارة · مسابقات لحظية · تحقق سيرفر-سايد · رفع صوتيات |

**الأثر:** لا اقتباس مُختلق، ولا مرجع وهمي، ولا نص محمي منسوخ بالكامل.

---

## 7) تقييم الجودة النهائي

| المحور | التقييم |
|---|---|
| التنظيم | ممتاز — 30 ملفًا مقسّمة بمنطق واضح |
| الاعتمادية | ممتاز — صفر مكتبات خارجية · صفر مفاتيح · يعمل دون إنترنت |
| الأمان | جيد جدًا **لما هو ممكن في موقع ثابت** — والقيود معلنة لا مخفية |
| الأمانة العلمية | عالٍ — كل شيء إما موثّق أو معلَّم بوضوح |
| التركيز المطلوب (الألحان) | ممتاز — الألحان هي أكبر قسم وأكبر تصنيف أسئلة |
| ملاءمة الشباب | عالٍ — 6 ألعاب + XP + 18 شعارًا + واجهة موبايل أولًا |
| القابلية للتوسّع | جيدة — إضافة محتوى = إضافة عنصر في ملف بيانات، أو من اللوحة |

---

**⛪ الخلاصة: من ملف واحد بمفتاح API → منصة منظمة، آمنة، صادقة، وبلا أي تكلفة — مع الحفاظ الكامل على كل ما كان يعمل.**
