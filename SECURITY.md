# 🛡 سياسة الأمان — البوت القبطي التعليمي (خادم)

> هذا الملف يشرح بدقة ما هو مؤمَّن في المشروع، وما لا يمكن تأمينه في موقع ثابت.
> الشفافية جزء من المشروع: «معلومة موثّقة أو لا معلومة» — وكذلك في الأمان.

---

## 1) ✅ ما هو مؤمَّن فعليًا

| البند | التنفيذ |
|---|---|
| **لا مفاتيح API في الكود** | لا يوجد أي مفتاح Gemini أو OpenAI أو توكن بوت في أي ملف. يوجد اختبار آلي في `tests.html` يفشل لو ظهر `AIza` أو مفتاح. |
| **لا استدعاء خدمات خارجية** | المساعد `js/core/bot.js` محلي 100%: يجيب من قاعدة معرفة داخل المشروع ولا يتصل بأي endpoint. |
| **تهريب كل نص ديناميكي (منع XSS)** | كل إدراج يمرّ عبر `Util.esc()` / `UI.*` / `UI.rich()` (تهريب قبل التنسيق). أسماء اللاعبين، الملاحظات، نتائج البحث، رسائل الاقتراحات… كلها مهرَّبة. |
| **منع روابط خطرة** | `Util.safeUrl()` يقبل فقط `https:` / `http:` / `#` / `mailto:` / `tel:` — ويرفض `javascript:` و `data:`. |
| **حدود طول لكل الحقول** | `APP.LIMITS` تحدد أطوال: الاسم، كود الغرفة، البحث، السؤال، الرسالة، الملاحظة. لا يمكن إدخال نص ضخم يكسر الواجهة. |
| **لا إرسال بيانات شخصية** | الإحصائيات محلية فقط، وبمعرّف جلسة عشوائي. لا اسم حقيقي ولا إيميل ولا IP. |
| **تنظيف المفاتيح القديمة** | عند أول تشغيل يمسح المشروع أي مفتاح Gemini قديم محفوظ في متصفحك من النسخ السابقة (`Store.purgeLegacySecrets`). |
| **مقاومة غش بسيطة** | `Bank.public()` يحذف الإجابة الصحيحة قبل إرسال السؤال للواجهة، ويُحدّد الحساب داخل المحرك لا في الصفحة. |
| **عدم تجاوز المصادر** | لا نحاول تجاوز تسجيل دخول أو صلاحيات أو مجموعات خاصة أو قيود robots. المصادر مُعلَّمة `searchable:false`. |

---

## 2) ⚠️ ما **لا يمكن** تأمينه في موقع ثابت (تنبيه صريح)

هذه ليست نواقص مخفية — بل قيود بنيوية، ومذكورة داخل الواجهة أيضًا:

1. **لا مصادقة حقيقية للوحة الإدارة.**
   أي بوابة تحقق مكتوبة بـ JavaScript يمكن قراءتها من كود الصفحة (Ctrl+U أو أدوات المتصفح).
   → لذلك لوحة `#/admin` **أداة تنظيم محلية** فقط لتجهيز المسودات. **لا تضع فيها أي كلمة سر أو مفتاح أو بيانات شخصية.**

2. **لا أدوار آمنة (Roles).**
   `APP.ROLES` موجودة كتنظيم للواجهة فقط وليس لها أي قيمة أمنية.

3. **لا تحقق سيرفر-سايد لنتائج المسابقات.**
   أي نتيجة تُخزَّن في المتصفح يمكن نظريًا تعديلها قبل الإرسال. لذلك لا نعتمد النقاط كمسابقة رسمية.

4. **لا أسرار ولا توكنات.**
   لا يمكن تخزين سر في موقع ثابت — ولهذا المساعد لم يعد يستخدم أي مفتاح أصلًا.

5. **لا معالجة وسائط.**
   لا رفع ملفات/صوتيات ولا تفريغها.

> **القاعدة:** لو احتجت أي بند من هذه الخمسة، فالحل الصحيح هو خدمة خلفية بمصادقة حقيقية — وليس بوابة JavaScript.

---

## 3) 🧹 للمستخدم: كيف تحمي نفسك

- لا تُدخل أي كلمة سر أو مفتاح في أي حقل بالموقع (لا نحتاجها ولا نطلبها).
- استخدم اسمًا مستعارًا في لوحة المتصدرين (لا تكتب اسمك الكامل أو رقم هاتفك).
- من صفحة **ملفي** يمكنك: مسح أي مفاتيح قديمة محفوظة، أو تصفير كل بياناتك المحلية.
- الموقع يعمل دون إنترنت — وبدون إنترنت لا يمكن أي تسريب بيانات حتى لو حدث خطأ.

---

## 4) 👷 للمطوّر: قائمة تحقق قبل أي تعديل

- [ ] أي إدراج نص جديد يستخدم `Util.esc()` أو `UI.*` — لا `innerHTML` خام لمحتوى خارجي.
- [ ] أي رابط جديد يمرّ عبر `Util.safeUrl()`.
- [ ] أي حقل إدخال جديد له حد في `APP.LIMITS`.
- [ ] لا تُضف `<script>` مضمّنًا طويلًا (استخدم ملفًا في `js/`).
- [ ] لا تضف أي مفتاح أو توكن — ولا `localStorage` لأسرار.
- [ ] أي معلومة غير مؤكدة تُعلَّم `NEEDS_ADMIN_INPUT`.
- [ ] شغّل `tests.html` وتأكد أن **كل** الاختبارات ناجحة (بما فيها اختبارات الأمان).

---

## 5) 🔄 تنبيه مهم عند التحديث (Cache)

بعد أي تحديث للملفات، اعمل **Cache reload** (`Ctrl+Shift+R` أو على الموبايل: إغلاق التبويب وفتحه من جديد) لتضمن أنك ترى النسخة الجديدة.

---

**⛪ الأمان هنا يعني: لا كذب، لا تخمين، ولا وعود بحماية لا يمكن لموقع ثابت أن يقدمها.**
